{
    "presets": [
      "@babel/preset-env",
      ["@babel/preset-react", {
        "runtime": "classic"  // Use classic runtime instead of automatic
      }],
      "@babel/preset-typescript"
    ]
  }